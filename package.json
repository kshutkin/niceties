{
    "private": true,
    "repository": {
        "type": "git",
        "url": "git+https://github.com/kshutkin/niceties.git"
    },
    "scripts": {
        "build": "pnpm --aggregate-output --reporter=append-only --stream -r build",
        "test": "pnpm --aggregate-output --reporter=append-only --stream --parallel -r test",
        "lint": "biome check .",
        "lint:fix": "biome check --write .",
        "ci:publish": "changeset publish"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.3.2",
        "@changesets/cli": "^2.29.7",
        "@types/jest": "^29.5.14",
        "@types/node": "^15.14.9",
        "jest": "^29.7.0",
        "pkgbld-internal": "~1.9.0",
        "pkgbld-plugin-dts-buddy": "^0.2.0",
        "ts-jest": "^29.4.5"
    }
}
