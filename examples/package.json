{
  "name": "examples",
  "version": "1.0.0",
  "description": "Examples for @liceties/logger and basic integration tests",
  "main": "index.js",
  "scripts": {
    "test": "node ./test-filter.mjs > ./result.log && node ./check-result.mjs",
    "examples": "node --expose-gc ./index.mjs  && node --expose-gc ./index.js"
  },
  "author": "Konstantin Shutkin",
  "license": "MIT",
  "private": "true",
  "dependencies": {
    "@niceties/logger": "^1.0.0",
    "@niceties/draftlog-appender": "^1.0.0",
    "kleur": "^4.1.4"
  }
}
